{
  "name": "aetheris-permadeath-record",
  "private": true,
  "scripts": {
    "test": "npm run test:static && npm run test:unit && npm run test:integration && npm run test:e2e",
    "test:static": "node scripts/check-syntax.mjs && node --test scripts/tests/static/*.test.mjs",
    "test:unit": "node --test scripts/tests/unit/*.test.mjs",
    "test:integration": "node --test scripts/tests/integration/*.test.mjs",
    "test:e2e": "playwright test",
    "smoke:leaderboard": "node scripts/leaderboard-smoke.mjs",
    "ci:verify": "npm run test:static && npm run test:unit && npm run test:integration && npm run smoke:leaderboard && npm run test:e2e && npm audit --audit-level=high",
    "ci:netlify": "npx playwright install chromium && npm run ci:verify"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.4",
    "@netlify/functions": "^5.2.0",
    "sql.js": "^1.14.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
